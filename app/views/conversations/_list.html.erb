<% if @conversations.size > 0 %>
  <% @conversations.each do |conversation| account = get_account_from_conversation(conversation) %>
    <% if account.present? %>
      <div class="row">
        <div class="col open_conversation" data-id="<%= account.id %>">
          <%= profile_thumbnail conversation.account %>
          <strong><%= conversation.account.first_name %></strong>
          <%= simple_format conversation.messages.last %>
        </div>
      </div>
    <% end %>
  <% end %>
<% else %>
  <p>No messages to display</p>
<% end %>
